package(default_visibility = ["//visibility:public"])
java_library(

    name = "jsonata",
    srcs = [
        "//schema-rules/src/main/java/io/confluent/kafka/schemaregistry/rules/jsonata:sources"
    ],
    deps = [
        "//schema-rules:main_dependencies"
    ],
    runtime_deps = [
        
    ],
    
)

sources()
